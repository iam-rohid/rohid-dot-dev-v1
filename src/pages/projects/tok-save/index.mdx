import ProjectLayout from "@src/components/layouts/ProjectLayout";
import meta from "./index.ts";
export default ({ children }) => (
  <ProjectLayout meta={meta}>{children}</ProjectLayout>
);

TokSave is a reposting app for TikTok. It allows users to save their favorite videos without watermark. It also allows users to bookmark their favorite videos and profiles and many more features.

TokeSave is built with React Native and Expo. It is only available on iOS for now.

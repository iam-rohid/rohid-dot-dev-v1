import React from "react";

const TagsPage = () => {
  return (
    <div>
      <h1>Tags</h1>
      <ul>
        {/* {tags.map((tag) => (
          <li key={tag.slug}>{tag.name}</li>
        ))} */}
      </ul>
    </div>
  );
};

export default TagsPage;

// TODO: Tags
